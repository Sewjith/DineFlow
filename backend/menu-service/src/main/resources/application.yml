spring:
  application:
    name: menu-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:dineflow}
    username: ${DB_USER:dineflow}
    password: ${DB_PASSWORD:dineflow}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
  servlet:
    multipart:
      # Photo uploads: reject anything larger before it reaches the service.
      max-file-size: 2MB
      max-request-size: 2MB

server:
  port: ${SERVER_PORT:8081}

# Shared JWT secret — must match the gateway's app.jwt.secret.
app:
  jwt:
    secret: ${JWT_SECRET:dev-local-secret-please-change-me-32chars-min}

logging:
  level:
    org.hibernate.SQL: info
